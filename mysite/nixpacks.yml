version: 1

build:
  environment:
    PYTHON_VERSION: "3.9.11"
  steps:
    - run: python3.9 -m venv /opt/venv
    - run: . /opt/venv/bin/activate && pip install -r requirements.txt

start:
  command: bash -c "cd ./mysite && python manage.py migrate && gunicorn mysite.wsgi"
